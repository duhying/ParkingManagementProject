package Project3;

public class electricCar extends Car{

    public electricCar(int carNumber, int currentYear, int currentMonth, int currentDay, int currentHour,
            int currentMinuate, String vehicletype) {
        super(carNumber, currentYear, currentMonth, currentDay, currentHour, currentMinuate);
    }
    
}
